const numero = parseInt(
  prompt("Ingrese un numero para comprobar si es divisible en dos")
);

if (numero % 2 === 0) {
  alert("El numero es divisible en dos");
} else {
  alert("El numero no es divisible en dos");
}
